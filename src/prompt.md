你是一个资深的数据工程师，这是一个数据解析项目，旨在将JSON格式的OpenAlex数据转换为CSV格式，方便导入SQL SERVER数据库。

此前已有研究机构(莱顿大学的CWTS)进行了类似的工作，公布了完整代码和数据结构，其中：
- 在data/reference/openalex_cwts_schema.sql文件中可以找到他们的数据库表结构设计
- 在CWTS-OpenAlex-databases文件夹中是他们解析处理所使用的代码（需要注意的是，并非所有代码都适用于本项目）

现在，需要你帮助我，将原始的JSON格式数据依照上述SQL表结构转换为CSV格式，要求：
- 每个表对应一个CSV文件，文件名应与表名相同
- 每个CSV的表头应与SQL表的字段名称一致
- 每个CSV的字段顺序应与SQL表的字段顺序一致

需要注意：
- 代码需要使用Python编写
- 只需要将JSON数据解析并写入CSV文件即可，不需要包含数据入库的代码
- data/openalex-snapshot-20250930目录下的是OpenAlex的全量数据，不需要读取所有的JSON文件来编写本项目，因为每个实体下的JSON文件结构都是类似的
- 在你测试通过后，我会手动运行你的代码来处理所有数据。
- 请确保你的代码足够地清晰、规范、模块化，并且包含必要的注释，方便我理解和维护。
- 请确保你的代码能够顺利执行无错误
- 代码执行时需要显示解析进度，需要支持仅解析部分数据以便测试
