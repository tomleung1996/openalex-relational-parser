这是一个数据解析项目，旨在将JSON格式的OpenAlex数据转换为CSV格式，方便导入SQL SERVER数据库。

此前已有研究机构进行了类似的工作，他们的代码并未公开，但公布了完整的数据。在data/reference/openalex_cwts_schema.sql文件中可以找到他们的数据库表结构设计。

现在，需要你帮助我，将原始的JSON格式数据依照上述SQL表结构转换为CSV格式。请确保生成的CSV文件符合SQL SERVER的导入要求。每个表对应一个CSV文件，文件名应与表名相同，每个CSV文件应包含所有必要的字段。

需要注意的是，data/openalex-snapshot-20250930目录下的是OpenAlex的全量数据，你不需要读取所有的JSON文件来编写本项目，因为每个实体下的JSON文件结构都是类似的。在你测试通过后，我会手动运行你的代码来处理所有数据。

请确保你的代码足够地清晰、规范、模块化，并且包含必要的注释，方便我理解和维护。代码需要使用Python编写。
